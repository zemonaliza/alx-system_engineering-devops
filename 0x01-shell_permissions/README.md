#!/bin/bash
chmod = change mode
sudo = super user do
su = switch user
chown = change owner 
chgrp = change group
id = show id
groups = show groups
whoami= show current user
adduser = add user
useradd = user add
addgroup = add group
